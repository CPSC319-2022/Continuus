steps:
  # Docker Build
  - name: 'gcr.io/cloud-builders/docker'
    entrypoint: 'bash'
    args: ['-c', 'docker build . --tag $BRANCH_NAME']  
    waitFor: ['-']
  
  - name: 'gcr.io/cloud-builders/docker'
    entrypoint: 'bash'
    args: ['-c', 'docker tag continuus:latest northamerica-northeast2-docker.pkg.dev/automatic-bot-376307/continuus/$BRANCH_NAME']

  # Docker Push
  - name: 'gcr.io/cloud-builders/docker'
    entrypoint: 'bash'
    args: ['-c', 'docker push northamerica-northeast2-docker.pkg.dev/automatic-bot-376307/continuus/$BRANCH_NAME']

options:
  logging: CLOUD_LOGGING_ONLY